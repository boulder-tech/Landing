interface Props {
    number: string,
    title: string,
    description:string,
}

const InvestorCard = ({number, title, description}:Props) => {

    return(
        <div>
            
        </div>
    )

};